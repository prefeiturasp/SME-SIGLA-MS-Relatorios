"""Módulo apps."""

from django.apps import AppConfig


class RelatoriosConfig(AppConfig):
    """Representa RelatoriosConfig."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "relatorios"
